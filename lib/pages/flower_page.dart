import 'package:flutter/material.dart';

class FlowerPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:Center(child: Text('订花页面'),)
    );
  }
}